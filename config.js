module.exports =
{
    using_freeroam: true,
    max_characters: 1000, // Maximum characters in a single chat message
    local_distance: 500, // Maximum distance for local chat
    default_channels: // Channels that chat will have
    [
        "Global",
        "Local",
        "Other"
    ]
}